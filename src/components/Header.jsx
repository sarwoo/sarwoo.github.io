import React from "react";

const Header = () => {
    return (
        <header>
            <h1 className="header-title">HEAD: dev-site by sarwoo</h1>
        </header>

    )
};

export default Header;